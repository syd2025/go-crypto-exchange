syntax = "proto3";

package market;
option go_package="./market";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Market {
  rpc Ping(Request) returns(Response);
}
